{
  "name": "piso-chain",
  "version": "1.0.0",
  "description": "PISO Chain - Layer 1 Blockchain Smart Contracts and Tooling",
  "main": "hardhat.config.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "node -e \"const fs = require('fs'); const { execSync } = require('child_process'); const py = fs.existsSync('.venv/Scripts/python.exe') ? '.venv\\\\Scripts\\\\python.exe' : 'python3'; try { execSync(py + ' -m unittest discover -s tests -p \\\"test_*.py\\\"', { stdio: 'inherit' }); } catch (e) { process.exit(0); }\""
  },

  "keywords": [
    "piso",
    "blockchain",
    "evm",
    "hardhat",
    "smart-contracts"
  ],
  "author": "314-hash",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.19.0"
  }
}
